from calibre.web.feeds.news import BasicNewsRecipe

class DigitalApplied(BasicNewsRecipe):
    title = 'Digital Applied'
    oldest_article = 7
    compress_news_images = True
    scale_news_images = (1264, 1680)
    language = 'en'
    requires_version = (9, 0, 0)
    auto_cleanup = True

    # keep_only_tags = []
    # remove_tags = []

    # FIXME: find RSS feed URL
    feeds = [
        ('Digital Applied', 'https://digitalapplied.com/feed'),
    ]

    # For review: open the generated .html with `open <slug>/for_review.html`
